#include "main.h"

/**
 * _strcmp.c - a function that compares two strings
 *
 * @s1: is an input to a string
 * @s2: is an input to a string
 *
 * Return: return 0
 */
int _strcmp(char *s1, char *s2);
{
	for (; (*s1 != '\0' && *s2 != '\0') && (*s1 == *s2); s1++, s2++)

	if (str1 == str2)
	{
	return (0);
	}

}
