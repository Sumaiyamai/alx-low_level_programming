#include "main.h"

/**
 *is_upper - if  uppercase character return 1, if not return 0,
 *
 * @c this is  entry
 *
 * return 0,
 */


	int _isupper(int c)
	
	int = c
{

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	return (0);
}
